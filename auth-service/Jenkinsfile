pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                checkout scm
            }
        }

        stage('Build') {
            steps {
                dir('auth-service') {
                    sh """
                    docker build -t auth-service:${BUILD_NUMBER} .
                    """
                }
            }
        }
    }
}
